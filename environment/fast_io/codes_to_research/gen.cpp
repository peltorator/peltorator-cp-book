#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("inp", "w", stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int T = 1e7;
    cout << T << endl;
    for (int i = 0; i < T; i++) {
        cout << i << ' ';
    }
    cout << endl;
    cerr << "Time: " << clock() * 1.0 / CLOCKS_PER_SEC << endl;
}
